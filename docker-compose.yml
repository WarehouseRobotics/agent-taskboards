services:
  taskboards:
    build:
      context: .
    environment:
      TASKBOARDS_DEBUG: "${TASKBOARDS_DEBUG-1}"
    ports:
      - "8142:8142"
    volumes:
      - .:/app
      - taskboards_node_modules:/app/node_modules

volumes:
  taskboards_node_modules:
